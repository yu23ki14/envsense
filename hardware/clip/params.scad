// envsense クリップ筐体 — パラメータ定義 (Phase 1 / #2)
//
// 値の単一ソースは dimensions.md。ここの「実測値」変数名は dimensions.md の
// 「OpenSCAD 変数名」列と完全一致させること（hardware/CLAUDE.md の規約）。
//
// 座標系（dimensions.md の規約に準拠）:
//   原点  = XIAO メイン基板の USB-C 側短辺の角
//   +X    = 基板長手方向（USB-C 端 → 反対端）, 0..board_l
//   +Y    = 基板幅方向, 0..board_w
//   +Z    = 厚み方向。z=0 はメイン基板の上面（USB-C 側）。
//           +Z = 上面（USB-C 側）/ -Z = 下面（Sense 拡張ボード・カメラ側）
//
// 「実測値」と「はめ合い後の値(_fit)」は分離する。実測は素のまま、
// クリアランスはここで足す（dimensions.md の測定ルール）。

// ===== 1. XIAO ESP32S3 メイン基板（実測値） =====
board_l      = 21.25;   // 基板 長さ
board_w      = 17.6;    // 基板 幅
pcb_t        = 1.15;    // PCB 厚み
corner_r     = 1.5;     // 角 R
usb_overhang = 1.6;     // USB-C コネクタの基板からの飛び出し（実測; 全長7.3 - 載り5.7。旧0.85を置換）
usb_w        = 8.9;     // USB-C コネクタ 幅
usb_h        = 3.0;     // USB-C コネクタ 高さ（筐体内 最高部品）
usb_open_w   = 8.3;     // USB-C 開口 幅（受け口 実測）
usb_open_h   = 2.5;     // USB-C 開口 高さ（受け口 実測）
usb_z        = 0.2;     // USB-C 開口 下端の基板上面からの高さ
top_clr      = 3.0;     // 上面 最大部品高さ（= usb_h）
pin_pitch    = 2.54;    // ピン ピッチ（規格）

// ===== 2. Sense 拡張ボード（実測値, ローカル原点） =====
sense_l     = 15.4;     // 外形 長さ
sense_w     = 17.6;     // 外形 幅
sense_t     = 1.1;      // PCB 厚み
b2b_gap     = 2.7;      // B2B コネクタ高さ（基板間の隙間）
mic_xy      = [6.6, 2]; // PDM マイク穴 位置（拡張ボード ローカル）
cam_fpc_xy  = [8.8, 9]; // カメラ FPC コネクタ 位置（拡張ボード ローカル）
sd_protrude = 2.5;      // SD カード挿入時の頭(-X)突き出し: 先端 x = -2.5（基板頭 x=0 基準, 実測）
                        //   旧記載「埋め込み式 0」は誤り。USB 前面(x=-1.6)よりさらに 0.9mm 出る

// ===== 3. 合体状態（実測値） =====
assy_h      = 7.8;          // 合体時 総厚（カメラ除く）
assy_offset = [6.3, 0];     // 拡張ボードの主基板に対するズレ X/Y

// ===== 4. OV3660 カメラモジュール（実測値） =====
cam_l    = 7.8;             // カメラ基板 長さ
cam_w    = 7.8;             // カメラ基板 幅
cam_t    = 1.9;             // カメラ基板 厚み
lens_d   = 6;               // レンズホルダー外径（= 前面レンズ穴径の基準）
lens_h   = 3.35;            // レンズホルダー突き出し高さ
lens_xy  = [3.9, 3.9];      // レンズ光軸（カメラ基板中心からのオフセット）
fpc_w    = 6;               // FPC 幅
fpc_l    = 8.7;             // FPC 長さ

// ===== 5. バッテリー（実測値） =====
bat_l       = 32.7;         // セル本体 長さ（基板長 21.25 より長い → レイアウト注意）
bat_w       = 20;           // セル本体 幅
bat_t       = 7.6;          // セル本体 厚み（最厚部）
bat_pcm_l   = 6.6;          // PCM 張り出し
bat_wire_d  = 0.7;          // リード線 線径

// =====================================================================
// 設計パラメータ（はめ合い・壁厚・造形）— ここを OEM のプロセスに合わせて調整
// =====================================================================

// 造形プロセス別の既定値の目安:
//   FDM      : wall 1.6 / clr 0.3
//   SLA/MJF  : wall 1.0 / clr 0.15
clr        = 0.3;   // 部品まわりの一般クリアランス（片側）
wall       = 1.6;   // 一般壁厚
wall_touch = 1.6;   // タッチ面（カメラ側 = 下面）の壁厚。静電容量が抜けるよう 1.5–2.0
lip_h      = 2.0;   // 本体/蓋の合わせ目リップ（印籠）の高さ
lip_t      = 1.0;   // リップの厚み
lip_clr    = 0.30;  // リップのはめ合いクリアランス（FDM 実機で 0.15 はきつく溝縁が変形 → 一般 clr と同値に）

// ===== 基板リテンション（4隅クランプ）=====
// 実測: メイン基板の4隅とも部品なしマージン 1.8mm（角は corner_r の R）。
//   Sense 拡張ボードには余白が無いので一切触れない。後(Sense側)2隅の上押さえは
//   B2B 隙間(b2b_gap)内に収め、メイン基板の上面(z=0)だけを押さえる
//   （Sense 下面 z_sense_bot=b2b_gap には当てない）。
//   基板はバッテリー基準のキャビティより小さく宙に浮くので、隅クレードルが
//   Z 受け（棚）と XY 位置決め（角の R を clr 付きで抱く）を兼ねる。
board_keepout = 1.8;   // 実測: 4隅の部品なしマージン（参考・チェック用）
clamp_reach   = 1.0;   // 基板縁への被さり量。clamp_reach + clr < board_keepout を守る
clamp_run     = 2.5;   // 角から各辺に沿うグリップ長さ（USB/カメラ等を避ける範囲）
shelf_t       = 0.8;   // 下受け棚の厚み(z)。bat_gap 以下に収める
top_h_front   = 2.5;   // 前(USB側)2隅の上押さえ高さ。上方が空くので高め可
// 後(Sense側)2隅の上押さえは廃止（旧 top_h_rear）: Sense が主基板尾を全幅で覆い、
//   蓋の垂直降下で Sense 上面に衝突して閉じられないため。浮き上がりは tail_backstop
//   のひさしで制限する（enclosure.scad の corner_grips / tail_backstop 参照）。
board_top_gap = 0.5;   // 押さえ爪を上へ平行移動する量＝基板上面との隙間（爪厚 top_h は維持）。
                       //   後隅の爪は廃止済みなので実質前(USB側)2隅のみに効く
weld          = 0.6;   // グリップを壁へ食い込ませる溶着代（union 確実化）
// 尾(+X)バックストップ: トップ半身所属の「天井吊り片持ちリブ ×2」（enclosure.scad の
//   tail_backstop 参照）。蓋を伏せて印刷したとき天井面から立ち上がる＝宙に浮く層が出ない。
//   中央開放が配線/アンテナの通り道とタッチ電極ポケット（pebble: cav_cy±6）の回避を兼ねる。
backstop_w = 7.3;      // リブの幅(Y)。+Y 壁(cav_y1 - clr)から内側へ伸びる単リブ
                       //   （単リブ化 2026-06: 配線の通り道を −Y 側にまとめて確保するため。
                       //   Sense 尾エッジとの受け面重なり 6.1mm ≥ 旧±Y 2リブ合計 4.6mm で
                       //   ストッパー機能は維持。内側端 y=11.5 はタッチ電極ポケット縁
                       //   （−Y 寄せ後 y=11.0）まで 0.5 残す上限）
backstop_t = 3.6;      // リブの厚み(X)。天井からの片持ちなので厚めに取って剛性確保

// はめ合い後の値（実測 + 余裕）
bat_t_fit  = bat_t + 1.0;   // バッテリー厚みは膨張ぶん +1mm（dimensions.md ルール）

// 部品配置の前提（要確認の設計判断 — 下記 TODO 参照）
// BAT パッド(基板裏=電池側)の半田＋リード線が電池に当たらないクリア。半田の山~1mm＋線ぶん。
bat_lead_clr = 1.6;   // 配線(半田＋細線)のための基板下面クリア
// 基板下面とバッテリー上面の仕切り。下受け棚(shelf_t)と配線クリア(bat_lead_clr)の大きい方。
bat_gap    = max(shelf_t + 0.2, bat_lead_clr);
// バッテリーは基板の下(-Z)に積層。bat_l=32.7 が基板長 21.25 より長いので
//   「頭合わせ」: バッテリー頭を基板頭(x=0)に揃え、尾(+X)へはみ出させる（#layout 決定）。
bat_x0     = 0;                    // バッテリー頭の x（基板頭に合わせる）
bat_y0     = board_w / 2 - bat_w / 2;  // 幅は中心合わせ
bat_off    = [0, 0];              // バッテリーの XY 微調整（bat_x0/bat_y0 基準）

// ===== Phase 1 追加計測（dimensions.md §7。実測で ASSUMED → MEASURED へ） =====
// 【画像準拠の修正】カメラは折り返した FPC で「上面(+Z)」に立ち、レンズは +Z（真上）へ。
//   位置は USB 端寄りで端をオーバーハング。FPC コネクタは逆端(遠端)。センサ = OV3660。
lens_axis    = [4.2, board_w / 2];                                    // MEASURED→実機補正: X=4.2(2.2から尾側(+X)へ2.0mm。実機で窓が頭側にずれていた。履歴: 3.5→2.2→4.2) / Y=幅中央
cam_org      = [lens_axis[0] - lens_xy[0], lens_axis[1] - lens_xy[1]]; // レンズ = カメラ基板中心
cam_tip_h    = 13.0;          // MEASURED 2026-06: 基板上面(z=0)→レンズ先端の総高。SD カード挿入時は
                              //   FPC 根本（折り返し）が SD カード上を通るため、カメラはここまで上がる
cam_base_z   = cam_tip_h - cam_t - lens_h;   // = 7.75。旧実測 usb_h+3.4(=6.4) は SD 干渉を考慮しない値
                                             //   → カード上面(5.0)との隙間 2.75mm を FPC 折り返しが使う
lens_front_d = 4.5;   // ASSUMED: レンズ前玉 有効径（開口・面取りの基準, < lens_d=6）
cam_fov      = 110;   // ASSUMED: OV3660 広角変種と推定(候補 64°/110°)。突出方式でケラレ影響は小
// マイク（試作は素の開口。防水メンブレンは後フェーズ）。部品面(+Z)に開口と仮定。
mic_pos       = [12.9, 3.0];   // MEASURED: X=12.9(長手,サブボード上) / Y=3.0(幅,端寄り)。X/Y入替を反映
mic_port_face = "top";     // ASSUMED: 部品面(+Z)に音孔。§7 で確定
mic_port_d    = 1.8;       // 試作の素開口径（1.0 は小さすぎ → 集音のため拡大）
// 干渉
usb_body_l = 5.7;     // MEASURED: USB端→コネクタ本体内側端

// ===== 派生：Z レイヤ（parts と enclosure で共有） =====
// 積層（上→下）: 上部壁 / カメラ / サブ基板(Sense) / メイン基板(XIAO) / バッテリー / 下部壁
// z=0 = メイン基板 上面（サブ基板を向く面）。+Z = 上（カメラ側） / -Z = 下（バッテリー側）
z_board_top = 0;                    // メイン基板 上面
z_board_bot = z_board_top - pcb_t;  // メイン基板 下面
// サブ基板(Sense) はメイン基板の上(+Z)、B2B 隙間ぶん持ち上がる
z_sense_bot = z_board_top + b2b_gap;
z_sense_top = z_sense_bot + sense_t;
// バッテリーはメイン基板の下(-Z)
z_bat_top   = z_board_bot - bat_gap;
z_bat_bot   = z_bat_top - bat_t_fit;
// 上面(+Z): カメラ（サブ基板の上に立つ）と USB コネクタ
z_usb_top   = z_board_top + usb_h;
z_cam_top   = cam_base_z + cam_t;   // カメラ基板 上面
z_lens_top  = z_cam_top + lens_h;   // レンズ先端（+Z 最高点）= 天面壁を貫通して突出

// ===== microSD カード（Sense 部品面(+Z)上のスロット。頭(-X)へ突き出す） =====
// スロットケージは Sense 拡張ボードの部品面(+Z)上（実機写真・Seeed wiki 写真で確認）。
// カードは USB 上面(z=3.0)とカメラ底(z=7.75)の間を通り、先端が x=-sd_protrude まで届いて
// 頭壁内面(cav_x0=-1.9)を 0.6mm 貫く → 頭壁に逃しが必要（enclosure の cut_sd / cut_sd_peb）。
sd_card_l = 15;    // カード規格 長さ
sd_card_w = 11;    // カード規格 幅
sd_card_t = 1.0;   // カード規格 厚み（リブ部最大）
sd_z0     = z_sense_top + 0.2;              // ASSUMED: カード下面 = Sense 上面 + ケージ床 0.2
sd_y0     = board_w / 2 - sd_card_w / 2;    // ASSUMED: 幅中央（実機写真でほぼ中央）
sd_clr_y  = 1.0;   // 逃し開口の y 余裕（片側）。z/y が ASSUMED のため広めに取る
sd_clr_z  = 0.8;   // 逃し開口の z 余裕（片側）

// ===== 派生：内部キャビティ寸法（基板＋バッテリーの footprint 包絡） =====
// 頭合わせレイアウト: 頭(-X)は USB 突出 or バッテリー頭の手前、尾(+X)は
//   基板尾 or バッテリー尾の奥。中心は両者の包絡中心（基板中心ではない）。
foot_x0 = min(-usb_overhang, bat_x0);          // 頭側 端
foot_x1 = max(board_l, bat_x0 + bat_l);        // 尾側 端
foot_y0 = min(0, bat_y0);
foot_y1 = max(board_w, bat_y0 + bat_w);
cav_l = (foot_x1 - foot_x0) + 2 * clr;
cav_w = (foot_y1 - foot_y0) + 2 * clr;
// 内部はカメラ本体まで内包。レンズは天面壁を貫通させて外へ出す。
top_headroom = 1.0;   // 天井の追加余白(z)。実機で基板(カメラ最上部)が top と干渉して閉まらなかった補正。
                      //   レンズ突出量はこの分だけ減る（z_lens_top - (天井+wall) = 0.45mm 残り）
cav_h = (max(z_cam_top, z_usb_top) - z_bat_bot) + 2 * clr + top_headroom;

// キャビティ中心（footprint 包絡の中心）
cav_cx = (foot_x0 + foot_x1) / 2;
cav_cy = (foot_y0 + foot_y1) / 2;

// キャビティ外縁（リテンション／リップ／開口の配置基準）
cav_x0 = cav_cx - cav_l / 2;  cav_x1 = cav_cx + cav_l / 2;
cav_y0 = cav_cy - cav_w / 2;  cav_y1 = cav_cy + cav_w / 2;

// 描画解像度。プレビューは粗く、書き出し(clip.scad の export)で上げる。
$fn = $preview ? 32 : 96;
