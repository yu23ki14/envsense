# 部品実測寸法メモ（Issue #1 / Phase 0）

XIAO ESP32S3 Sense + OV3660 カメラ + LiPo バッテリーの実測寸法。
Phase 1（#2）の OpenSCAD パラメトリック筐体設計の基準値。

## 測定ルール

- 単位は **mm**。デジタルノギス 0.05mm 単位、各寸法 3 回測って中央値を記録。
- **実測値は「素のサイズ」を記録する。** はめ合いのクリアランスは OpenSCAD 側で足す。
- **マスター原点 = XIAO メイン基板の USB-C 側短辺の角。** 主基板の部品はこの原点基準。
- 拡張ボード／カメラの X/Y は各ローカル原点基準。実空間へは `assy_offset` で変換。
- 上面 = USB-C 側 / 下面 = Sense 拡張ボードが付く側。

測定日: ____ / 測定者: ____

---

## 1. XIAO ESP32S3 メイン基板

| OpenSCAD 変数名 | 測る箇所 | 目安 | 実測値 | メモ |
| --- | --- | --- | --- | --- |
| `board_l` | 基板 長さ（USB-C 側→反対端） | 21.0 | 21.25 |  |
| `board_w` | 基板 幅 | 17.5–17.8 | 17.6 |  |
| `pcb_t` | PCB 単体の厚み | 1.0 | 1.15 |  |
| `corner_r` | 角のコーナー半径 | R1.0–1.5 | R1.5 | 幅 17.6・直線部 14.4 から算出 |
| `usb_overhang` | USB-C 突き出し量（基板端からの出っ張り） | ~1.0 | 1.6 | コネクタ全長7.3 − 基板載り5.7(`usb_body_l`)。旧0.85を更新 |
| `usb_w` | USB-C コネクタ 幅 | ~9.0 | 8.9 |  |
| `usb_h` | USB-C コネクタ 高さ | ~3.2 | 3.0 | 筐体内で最も高い部品 |
| `usb_open_w` | USB-C 開口 幅（穴の内側実測） | — | 8.3 |  |
| `usb_open_h` | USB-C 開口 高さ（穴の内側実測） | — | 2.5 |  |
| `usb_z` | USB-C 開口 下端の基板上面からの高さ | — | 0.2 |  |
| `top_clr` | 上面の最大部品高さ | — | 3.0 | USB-C が最高部品（= `usb_h`） |
| `bot_clr` | 下面の最大部品高さ | — | なし | B2B は `b2b_gap` で計上 |
| `btn_r_xy` | リセット(R)ボタンの位置 X / Y | — | 1.3 / 2.8 | 筐体に露出しない |
| `btn_b_xy` | ブート(B)ボタンの位置 X / Y | — | 14.6 / 2.8 | 筐体に露出しない |
| `btn_h` | ボタン頂部の高さ | — | — | 露出しないため不要 |
| `pin_edge` | 14 ピン列の左右端からの距離 | — | 16 | #51 Grove 配線の引き出し基準 |
| `pin_pitch` | ピン ピッチ | 2.54 | 2.54 | 規格値 |
| `ant_ufl_xy` | 外付けアンテナ U.FL コネクタの基板上位置 X / Y | — | 19.5 / 2.5 |  |
| `ant_l` | アンテナシート 長さ | — | 37.2 | 基板より長い。配置注意 |
| `ant_w` | アンテナシート 幅 | — | 17.2 | 厚みはシートのため無視可 |
| `ant_cable_l` | U.FL ピグテール（同軸）長さ | — | 55 |  |

---

## 2. Sense 拡張ボード（カメラ/マイク アダプタ基板）

> X/Y は拡張ボードのローカル原点基準。実空間へは `assy_offset` で変換。

| OpenSCAD 変数名 | 測る箇所 | 目安 | 実測値 | メモ |
| --- | --- | --- | --- | --- |
| `sense_l` | 外形 長さ | ~21.0 | 15.4 |  |
| `sense_w` | 外形 幅 | ~17.5 | 17.6 |  |
| `sense_t` | PCB 厚み | ~1.0 | 1.1 |  |
| `sd_xy` | microSD スロットの位置 X / Y | — | 記録不要 | スロットは部品面(+Z)上のケージ。筐体側はカード包絡（下記）基準で逃すため位置は不要 |
| `sd_protrude` | SD を挿したときの突き出し量 | — | 2.5 | **実測 2026-06**: カード先端 x = -2.5（基板頭 x=0 基準）。USB 前面(x=-1.6)より 0.9mm 出る。旧記載「埋め込み式 0」は誤り → 頭壁に逃しが必要（cut_sd / cut_sd_peb） |
| `sd_z0` | カード下面の z | — | 未測 | ASSUMED: z_sense_top + 0.2 ≈ 4.0（USB 上面 3.0 とカメラ底 7.75 の間）。要実測 |
| `sd_y0` | カードの y 範囲（幅 11） | — | 未測 | ASSUMED: 幅中央（board_w/2 ± 5.5）。要実測 |
| `b2b_gap` | B2B コネクタの高さ（= 2 枚の基板の隙間） | — | 2.7 |  |
| `mic_xy` | PDM マイク穴の位置 X / Y | — | 6.6 / 2 | #30 では防水メッシュ |
| `cam_fpc_xy` | カメラ FPC コネクタ（FPC 始点）の位置 X / Y | — | 8.8 / 9 |  |

---

## 3. 合体状態（XIAO ESP32S3 Sense アセンブリ）

| OpenSCAD 変数名 | 測る箇所 | 目安 | 実測値 | メモ |
| --- | --- | --- | --- | --- |
| `assy_h` | 合体時の総厚（メイン上面の最高部品→Sense 下面の最高部品） | — | 7.8 | カメラ除く。キャビティ高さの基準 |
| `assy_offset` | 拡張ボードの主基板に対するズレ X / Y | — | 6.3 / 0 | 拡張ボードが後端へ ~0.45mm 出っ張る（6.3+15.4=21.7 > `board_l` 21.25） |

---

## 4. OV3660 カメラモジュール

| OpenSCAD 変数名 | 測る箇所 | 目安 | 実測値 | メモ |
| --- | --- | --- | --- | --- |
| `cam_l` | カメラ基板 長さ | — | 7.8 |  |
| `cam_w` | カメラ基板 幅 | — | 7.8 |  |
| `cam_t` | カメラ基板 厚み | — | 1.9 |  |
| `lens_d` | レンズホルダー（円筒）外径 | — | 6 | 筐体前面のレンズ穴径 |
| `lens_h` | レンズホルダーの基板上面からの突き出し高さ | — | 3.35 | 前壁厚の基準 |
| `lens_xy` | レンズ光軸の位置（カメラ基板中心からのオフセット） | — | 3.9 / 3.9 | 基板中心 |
| `fpc_w` | FPC ケーブルの 幅 | — | 6 |  |
| `fpc_l` | FPC ケーブルの 長さ | — | 8.7 | 短いためカメラは拡張ボードのすぐ脇に固定 |

FPC 始点（拡張ボード上のコネクタ位置）は `cam_fpc_xy`（セクション 2）に統合。

---

## 5. バッテリー（LiPo セル）

リポは個体差・膨張があるため必ず実測。実測は素のまま記録し、厚みは OpenSCAD 側で +0.5〜1mm 余裕を足す。

| OpenSCAD 変数名 | 測る箇所 | 目安 | 実測値 | メモ |
| --- | --- | --- | --- | --- |
| `bat_l` | セル本体 長さ | — | 32.7 | 旧 25.5 を再計測で更新。**基板長 21.25 より長い → レイアウト注意** |
| `bat_w` | セル本体 幅 | — | 20 | 旧 19.5 |
| `bat_t` | セル本体 厚み（一番厚い所） | — | 7.6 | 旧 6.7 |
| `bat_pcm` | 保護回路(PCM)部の出っ張り | — | 厚み同じ / 張り出し 6.6 |  |
| `bat_wire_d` | リード線（赤黒）の線径（被覆込み） | — | 0.7 | 赤・黒それぞれ 1 本あたり |
| `bat_wire_l` | リード線の長さ | — | 50 |  |
| `bat_conn` | コネクタ種類・本体寸法 | — | コネクタなし（直結） |  |
| — | 型番・容量 | — | 300mAh | 記録用 |

接続方針: バッテリーは交換しない前提で **直結**。**物理スイッチは設けない**。
電源 ON/OFF は静電タッチ長押し → ディープスリープで行う（バッテリーは常時 `BAT` 接続＝充電は常に可能）。

---

## 6. 将来 issue 向けのメモ

### #51 Grove モジュラー化

- I2C で引き出すピン（SDA / SCL）: ____
- ピン列の引き出し位置: ____
- 接続方式（Grove Shield for XIAO / 自作キャリア基板）: 未定

### #30 防水

浸水経路になる開口（後でパッキン溝・シール設計の入力）:

| 開口 | 位置 | 数 |
| --- | --- | --- |
| USB-C | 主基板 USB 端 / 開口 8.3×2.5・下端 Z0.2 | 1 |
| R / B ボタン | 露出しない | 0 |
| マイク穴 | 拡張ボード基準 6.6 / 2 | 1 |
| SD スロット | **box**: 頭端面の貫通スロット（USB 開口と連結。閉じてから外挿し） / **pebble**: 露出しない（壁内の縦チャネル逃し、外面開口なし） | box 1 / pebble 0 |

### 静電タッチ長押し（電源 ON/OFF も兼ねる）

- ESP32S3 は GPIO1〜14 が容量タッチ対応。
- 役割: 長押しでディープスリープ ⇄ 復帰（＝電源 OFF/ON）。意図的キャプチャ等の操作もここに集約。
- タッチパッドに繋ぐピン: 未定（カメラ/マイク/SD で GPIO 逼迫、firmware と要調整）。タッチ起床は RTC GPIO 対応ピンが必要。
- タッチ面（壁厚を ~1.5〜2mm に薄くする面）: **カメラ側の面**。

### 設計メモ: 電源 ON/OFF（決定済み）

- **方式: 静電タッチ長押し → ディープスリープ。物理スイッチは設けない。**
- バッテリーは常時 `BAT` パッドに接続されるため、充電は電源状態によらず常に可能。
- ディープスリープ中の ESP32-S3 は数十 µA 程度で、実用上「OFF」とみなせる。
- 筐体に電源スイッチ用の開口が不要 → #30 防水で有利。

### 設計メモ: USB-C 開口

- USB ケーブルのプラグ樹脂モールドはケーブルごとに異なるため、特定寸法は測らない。
- 開口は受け口（8.3×2.5）より広め、一般的な USB-C プラグが挿せる ~12〜13mm 幅で設計し、USB 端の壁は薄く（または面取り）する。

---

## 7. Phase 1 追加計測（カメラ実装・マイク・干渉）

### 修正アセンブリ前提（実機写真で確定）

- カメラは折り返した **FPC で上面(+Z)に立ち、レンズは +Z（真上）** を向く。
- カメラ位置は **USB ポートの真上**（X: USB ポートと重なる位置 ≈ 3.5、Y = 幅中央 ≈ 8.8 = USB 差込口と同位置）。
  カメラは USB コネクタの上に重なって立つ（→ `cam_base_z` を USB 上面基準で測れる）。
- カメラ FPC の**白コネクタは逆端（遠端 = +X）**。
- **最高部品はカメラ**（USB-C ではない）。総高 ~15mm の主因。天面クリアランスはカメラ高さが支配。
- レンズ穴・マイク穴は **上面(+Z)**。USB-C 開口は短辺の **端面(-X)**。**底面(-Z)は開口なし**（バッテリー側）。
- センサは **OV3660**（旧記載 OV2640 は誤り）。

**計測はすべてマスター原点（XIAO USB-C 側短辺の角, z=0=メイン基板上面, +Z=上面）基準。**
向きの検算：R ボタン中心が **≈ (1.3, 2.8)** になれば座標系の向き正解。

### カメラ（開口貫通＝レンズ突出 で実装）

| OpenSCAD 変数名 | 測る箇所 | 実測値 | メモ |
| --- | --- | --- | --- |
| `lens_axis` | レンズ光軸の実空間 X / Y | ~3.5 / 8.8 | X=USBポートの真上(重なる) / Y=USB差込口と同位置(幅中央)。MEASURED |
| `cam_tip_h` | 基板上面(z=0)→レンズ先端の総高 | 13.0 | **実測 2026-06**: SD カード挿入時、カメラ FPC の根本（折り返し）が SD カード上を通るため、カメラはここまで上がる。MEASURED |
| `cam_base_z` | カメラ底面の z=0 からの高さ | 7.75 | = `cam_tip_h` − `cam_t` − `lens_h`（DERIVED）。旧実測 USB上面+3.4 (=6.4) は SD 非挿入状態の値で、挿入時は FPC 根本が SD と干渉 → +1.35 上がる |
| `lens_h`（検算） | カメラ基板上面→レンズ先端の突出 | | 既値 3.35 の再確認 |
| `cam_rot` | カメラの回転（基板に対し 0/90/180/270） | | FPC の折り方向 |
| `lens_front_d` | レンズ前玉の有効径（光が通る最小径） | | 開口・面取りの基準（< ホルダ径 6） |
| `cam_fov` | 画角（deg, OV3660 データシート可） | ~110(推定) | OV3660 広角変種(候補 64°/110°)。突出方式でケラレ影響小 |
| `cam_l/w/t`（検算） | カメラ基板 外形 | / / | 既値 7.8 / 7.8 / 1.9 の再確認 |

### マイク（試作は素の開口。防水メンブレンは後フェーズ）

> 写真上の位置は「**中央やや遠端・左端寄り**」（≈ `mic_xy`+`assy_offset` = 12.9 / 2）。

| OpenSCAD 変数名 | 測る箇所 | 実測値 | メモ |
| --- | --- | --- | --- |
| `mic_port_face` | 音孔が開く面（top=部品面+Z / bottom=基板裏 / 通孔） | | 音道の通し方を決定。暫定 top |
| `mic_port_d` | 部品側 音孔径 | | 外殻ポートの基準 |
| `mic_pos` | 音孔の実空間 X / Y | 12.9 / 3.0 | MEASURED（X=長手・サブボード上 / Y=幅 端寄り）。X/Y入替を反映 |

### 干渉・全体

| OpenSCAD 変数名 | 測る箇所 | 実測値 | メモ |
| --- | --- | --- | --- |
| `usb_body_l` | USB-C 本体の基板内側への入り込み長 | 5.7 | MEASURED |
| （検算） | レンズ先端までの総高（最低部→レンズ先端） | | 写真の ~15mm と整合するか |
