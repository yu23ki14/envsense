import { ModalPlaceholder } from '../src/components';

export default function JournalRoute() {
  return (
    <ModalPlaceholder
      title="日別ジャーナル"
      description="選択した日の日記・写真・セッションがここに表示されます。"
    />
  );
}
